import React from 'react';
export const metadata = {
  title: "All Lessons",
};

const AllLessonLayout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default AllLessonLayout;