import React from 'react';
export const metadata = {
  title: "LifeAtlas Register",
};

const RegisterLayout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default RegisterLayout;